#!/usr/bin/python3
import sys

args = sys.argv
if len(args) == 1:
    print("0 arguments.")
else:
    print("{:d} arguments:".format(len(args)))
    for i  in range(1,len(args)):
        print("{:d}: {:s}".format(i, args[i]))
